import JoinUsCard from '../joinUsCard/joinUsCard';
import { usePageTitle } from '../pageTitle/pageTitle';

export const JoinUsForm = () => {
  usePageTitle("Join Us - Raah e Karam");
  return (
    <JoinUsCard/>
  )
};
